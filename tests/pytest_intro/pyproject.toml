[project]
name = "pytest-intro"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "20100dbg", email = "20100dbg@world.net" }
]
requires-python = ">=3.12"
dependencies = [
    "pytest>=9.1.1",
    "pytest-mock>=3.15.1",
    "requests>=2.34.2",
]

[project.scripts]
pytest-intro = "pytest_intro:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
